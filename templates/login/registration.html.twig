{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}

<div class="container">
    <h1>Go ahead and register !!</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="submit btn btn-success">Go !!</button>
    {{ form_end(form) }}

</div>

{% endblock %}